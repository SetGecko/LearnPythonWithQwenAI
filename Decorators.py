'''
Что такое декораторы?
Декоратор — это специальная функция (или класс), которая позволяет "обернуть" другую функцию или метод,
добавляя к ней дополнительную функциональность без изменения её исходного кода.
Это мощный инструмент для рефакторинга и повторного использования кода.

Как работают декораторы?
Обертка над функцией:
Декоратор получает функцию как аргумент, выполняет дополнительные действия
до/после вызова этой функции и возвращает обновленную версию функции.
Синтаксис:
Простой синтаксис: @decorator_name.
Альтернативный способ: function = decorator_name(function).

Основные типы декораторов
    Декораторы для функций:
Используются для добавления поведения к обычным функциям.
    Декораторы для методов классов:
Позволяют изменять поведение методов класса.
    Декораторы с параметрами:
Можно передать дополнительные настройки для декоратора.

Зачем нужны декораторы?
Рефакторинг:
Добавляешь общее поведение (например, логирование, проверку прав) без дублирования кода.
Чистота кода:
Оригинальная функция остаётся чистой, а дополнительная логика помещается в декоратор.
Расширяемость:
Можно легко добавить новый декоратор, не меняя основной код.
'''
# Дополнительная информация о декораторах
'''
Почему log принимает func?
Потому что декоратор — это функция, которая принимает другую функцию (func), чтобы "обернуть" её в wrapper.
Что такое wrapper?
Это функция-обёртка , которая выполняет дополнительные действия (логирование, проверки) перед и после вызова декорируемой функции.
Почему wrapper принимает *args и **kwargs?
Чтобы обёртка могла работать с любыми параметрами декорируемой функции (например, add(5, 3) принимает a и b).
Зачем возвращать wrapper?
Чтобы декоратор заменил оригинальную функцию на wrapper, и при вызове функции выполнялась обёртка.
Декораторы часто используются?
Да! Например:
@property для создания вычисляемых атрибутов.
@admin_required для проверки прав доступа.
@timer для измерения времени выполнения.
@property — это декоратор?
Да! @property — встроенный декоратор, который превращает метод в атрибут класса.

Как это работает:
@log — это сокращение для:
python
Copy
1
add = log(add)  # Декоратор log принимает функцию add и возвращает wrapper
При вызове add(5, 3) ты фактически вызываешь wrapper(5, 3) , который:
Печатает лог.
Вызывает add(5, 3) (оригинальную функцию).
Печатает результат.
Возвращает результат.


1. Основная функция: say_hello()
Сначала создадим обычную функцию, которая выводит "Hello!":
def say_hello():
    print("Hello!")

2. Декоратор: "Обертка" для добавления приветствия
Теперь создадим декоратор @add_greeting, который будет:

Перед вызовом say_hello() печатать "Привет!".
После вызова печатать "До встречи!".
def add_greeting(func):
    def wrapper():
        print("Привет!")  # Добавляем действие ДО оригинальной функции
        func()             # Вызываем оригинальную функцию
        print("До встречи!")  # Добавляем действие ПОСЛЕ оригинальной функции
    return wrapper

3. Применение декоратора
Обернем функцию say_hello декоратором @add_greeting:
@add_greeting
def say_hello():
    print("Hello!")

Это эквивалентно записи:
say_hello = add_greeting(say_hello)

Пошаговый разбор:
Функция say_hello просто выводит "Hello!".
Декоратор add_greeting :
Принимает func (в данном случае — say_hello).
Внутри определяет wrapper — новую функцию, которая:
Сначала печатает "Привет!".
Затем вызывает func() (оригинальный say_hello).
Потом печатает "До встречи!".
Возвращает wrapper вместо оригинальной функции.
При вызове say_hello() ты фактически вызываешь wrapper(), который добавляет дополнительные сообщения.

Почему это полезно?
Без декоратора :
def say_hello():
    print("Hello!")

print("Привет!")
say_hello()
print("До встречи!")

Это требует писать дополнительный код каждый раз.
С декоратором :
Дополнительная логика (приветствие и прощание) добавляется одним нажатием @add_greeting.

Еще проще?
Декоратор — это функция, которая "обрастает" дополнительным функционалом .
Как будто ты надел на нее магический шарф , который добавляет действия до и после.

Формула декоратора:
def decorator(func):
    def wrapper():
        # Действие до
        func()
        # Действие после
    return wrapper

Твой эксперимент:
Попробуй изменить декоратор, чтобы он добавлял смайлики:
def add_smiles(func):
    def wrapper():
        print("😊")  # Добавь смайлик до
        func()
        print("😭")  # Добавь смайлик после
    return wrapper

@add_smiles
def say_hello():
    print("Hello!")

say_hello()
'''

'''
Задача 1: Декоратор для добавления смайлика
Описание:
Создай декоратор @add_smile, который добавляет смайлик 😊 в конец строки, возвращаемой функцией.

Алгоритм:
Создай функцию-декоратор add_smile.
Внутри декоратора определи обёртку wrapper.
В wrapper вызови декорируемую функцию.
К результату добавь смайлик 😊.
Верни обёртку из декоратора.
'''

'''
Задача 2: Декоратор для подсчета вызовов
Описание:
Создай декоратор @count_calls, который считает количество вызовов функции.

Алгоритм:
Создай функцию-декоратор count_calls.
Внутри декоратора определи переменную call_count = 0.
В обёртке wrapper увеличивай call_count на 1 при каждом вызове.
После вызова функции выводи сообщение: "Функция вызывалась X раз".
Верни обёртку из декоратора.
'''

'''
Задача 3: Декоратор для проверки числа
Описание:
Создай декоратор @check_positive, который заменяет отрицательные числа на 0 перед передачей их в функцию.

Алгоритм:
Создай функцию-декоратор check_positive.
В обёртке wrapper проверь аргумент: если он < 0, замени на 0.
Вызови оригинальную функцию с исправленным аргументом.
Верни результат.
'''

'''
Задача 4: Декоратор для добавления копирайта
Описание:
Создай декоратор @add_copyright, который добавляет строку "© Qwen & You" в конец строки, возвращаемой функцией.

Алгоритм:
Создай функцию-декоратор add_copyright.
В обёртке wrapper вызови оригинальную функцию.
К результату добавь "© Qwen & You".
Верни обёртку.
'''

'''
Задача 5: Декоратор для переворачивания строки
Описание:
Создай декоратор @reverse_string, который переворачивает строку, возвращаемую функцией.

Алгоритм:
Создай функцию-декоратор reverse_string.
В обёртке wrapper проверь, что результат функции — строка.
Если да — переверни её с помощью [::-1].
Верни обёртку.
'''